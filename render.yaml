services:
  - type: worker
    name: holding-channel-content-bot
    runtime: python
    plan: starter
    buildCommand: pip install -r requirements.txt
    startCommand: python -m app.main
    autoDeploy: true
